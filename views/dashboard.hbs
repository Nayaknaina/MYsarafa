<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sarafa Community Dashboard</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <link rel="canonical" href="https://app.mysarafa.com/">
</head>
<script src="https://cdn.tailwindcss.com"></script>
<style>
    .otp-input::-webkit-outer-spin-button,
    .otp-input::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
    }

    .otp-input[type="number"] {
        -moz-appearance: textfield;
        /* Firefox */
    }

    .otp-input {
        border-radius: 0.75rem;
        border-width: 2px;
        border-color: #e5e7eb;
        outline-style: none;
        width: 3.5rem;
        height: 3.5rem;
        font-size: 1.25rem;
        line-height: 1.75rem;
        font-weight: 600;
        text-align: center;
        transition-property: all;
        background-color: rgba(255, 255, 255, 0.8);
        transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
        transition-duration: 300ms;
        backdrop-filter: blur(4px);
    }

    .otp-input:focus {
        border-color: #3b82f6;
        box-shadow: var(--tw-ring-inset) 0 0 0 calc(4px + var(--tw-ring-offset-width)) var(--tw-ring-color);
        --tw-ring-color: #dbeafe;
        background-color: #ffffff;
        transform: scale(1.05);
    }

    .otp-input:not(:placeholder-shown) {
        border-color: #34d399;
        background-color: #ecfdf5;
    }

    [type="submit"]:disabled,
    button:disabled {
        opacity: 0.5;
        cursor: not-allowed;
           background-image: linear-gradient(to right, #2a4aa7, #224485);
        transform: scale(1);
        box-shadow: none;
        transition: none;
    }

    button:disabled:hover {
        background-image: linear-gradient(to right, #224485, #2a4aa7);
        transform: scale(1);
        box-shadow: none;
        transition: none;
    }
    .search-container {
            position: relative;
            margin-bottom: 1.5rem;
        }
        .grpsearch-input {
            width: 100%;
            padding: 0.35rem 1rem 0.35rem 1.75rem;
            border: 2px solid #e5e7eb;
            border-radius: 0.75rem;
            font-size: 1rem;
            outline: none;
            transition: all 0.3s ease;
        }
        .grpsearch-input:focus {
            border-color: #3b82f6;
            box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
        }
        .grpsearch-icon {
            position: absolute;
            left: 0.75rem;
            top: 50%;
            transform: translateY(-50%);
            color: #6b7280;
        }
        .leave-group-btn{
                color:#fff ;
                background-color: rgb(255, 46, 46);
                padding: 8px 16px;
                border-radius: 8px;
                border: none;
                cursor: pointer;
                transition: transform 0.3s;
                font-size: 12px;
        }
    @media (max-width: 768px) {
        .leave-group-btn{
            margin-left: 0px;
            margin-top: 10px;
        }
    }
.kyc-required-btn{
    background-color: #2f95ff;
    color: #fff;
    border: none;
    border-radius: 8px;
    padding: 10px 20px;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s, transform 0.3s;
    margin-top: 20px;   
    font-weight: 500;
margin-left: 35%;    
}
</style>

<body data-user-id="{{user._id}}">
    <!-- Header -->
    {{> header}}

    <div class="main-container">
        <!-- Sidebar -->
        {{> sidebar}}

        <!-- Main Content -->
        <main class="main-content">
            <h1 class="page-title">Dashboard</h1>

            {{#if pendingGroup}}
            <div id="duePaymentModal" class="modal-overlay" style="display: flex;">
                <div class="modal-content">
                    <div class="modal-header">
                        <span style="font-size: 18px; font-weight: 600;">Payment Due</span>
                        <button class="modal-close-btn" id="closeDuePaymentModal">&times;</button>
                    </div>
                    <div class="modal-body">
                        <p style="font-size: 15px; color: #333; margin-bottom: 18px;">
                            Pay your pending payment.<br>
                            Your <strong>{{pendingGroup.g_name}}</strong> payment of 
                            <strong>₹{{pendingGroup.amount}}</strong> is due.
                        </p>
                        <div style="display: flex; gap: 10px;">
                            <button id="payNowBtn" data-group-id="{{pendingGroup._id}}" style="flex: 1; background: #2f95ff; color: #fff; border: none; border-radius: 8px; padding: 10px 0; font-size: 15px; font-weight: 500; cursor: pointer;" class="pay-now-btn">Pay Now</button>
                        </div>
                    </div>
                </div>
            </div>
            {{/if}}

            <!-- Stats Cards -->
            <div class="stats-grid">
                <div class="stat-card blue-card">
                    <div class="stat-header">
                        <div class="stat-info">
                            <h3>Total Members</h3>
                            <div class="stat-number"></div>
                        </div>
                        <button class="add-new-btn" id="addNewBtn">Add New +</button>
                    </div>
                </div>

                <div class="stat-card blue-card">
                    <div class="stat-info">
                        <h3>Up coming Events</h3>
                        <div class="stat-text">3 Days before</div>
                    </div>
                </div>
            </div>

            <section class="group-discovery-section">
                <div class="group-discovery-container">
                    
                    <div class="discover-groups">
                        <div class="flex justify-between items-center mb-4">
                            <h2 class="section-title">Discover Community</h2>
                           <div class="search-container w-1/3">
                                <i class="fas fa-search grpsearch-icon"></i>
                                <input type="text" id="discoverSearch" class="grpsearch-input" placeholder="Search communities...">
                                <div id="discoverLoading" class="loading-spinner"></div>
                            </div>
                        </div>
                        <div id="discoverGroupsList">
                        <p id="discoverNoResults" style="display: none;">No public communities available to join.</p>
                            {{#each discoverGroups}}
                            <div class="group-card" data-group-name="{{this.g_name}}">
                                <img src="{{this.g_cover}}" alt="{{this.g_name}} Image" class="group-image">
                                <div class="group-details">
                                    <h3 class="group-title">{{this.g_name}}</h3>
                                    <p class="group-description">{{this.description}} Join this {{this.g_type}} community to connect with others.</p>
                                    {{#if isJoined}}
                                    <button class="joined-btn" disabled>Joined</button>
                                    {{else if isPending}}
                                    <button class="pending-btn" disabled>Pending</button>
                                    {{else}}
                                    <button class="join-group-btn" data-group-id="{{_id}}">Join Community</button>
                                    {{/if}}
                                </div>
                            </div>
                            {{/each}}
                        </div>
                    </div>

                    <!-- My Groups (User's own groups) -->
                    <div class="my-groups">
                        <div class="flex justify-between items-center mb-4">
                            <h2 class="section-title">My Communities</h2>
                            <div class="search-container w-1/3">
                                <i class="fas fa-search grpsearch-icon"></i>
                                <input type="text" id="myGroupsSearch" class="grpsearch-input" placeholder="Search my communities...">
                                <div id="myGroupsLoading" class="loading-spinner"></div>
                            </div>
                        </div>
                        <div id="myGroupsList">
                        <p id="myGroupsNoResults" style="display:none;">You haven't created or joined any communities yet.</p>
    
                        {{#each myGroups}}
                        <div class="group-card">
                            <img src="{{this.g_cover}}" alt="{{this.g_name}} Image" class="group-image">
                            <div class="group-details">
                                <h3 class="group-title">{{this.g_name}}</h3>
                                <p class="group-description">Manage your {{this.g_type}} community with
                                    {{this.total_mem}} members.</p>
                                {{!-- <button class="manage-group-btn" data-group-id="{{this._id}}">Manage Community</button> --}}
                                {{#if (eq this.membershipType "admin")}}
                                      <button class="manage-group-btn" data-group-id="{{this._id}}">Manage Community</button>
                                {{else}}
                                  <button class="leave-group-btn" data-group-id="{{this._id}}">Leave Community</button>
                                {{/if}}
                               
                                
                            </div>
                        </div>
                       
                        {{/each}}
                        </div>
                    </div>
                </div>
            </section>


            <!-- Past Events -->
            {{!-- <section class="section">
                <h2 class="section-title">Past events</h2>
                <div class="events-grid">
                    <div class="event-card">
                        <div class="event-content">
                            <img src="Assets/Images/Award.jpg" alt="event image" class="event-image">
                            <div class="event-details">
                                <h3 class="event-title">Yearly Award Ceremony</h3>
                                <div class="event-info">
                                    <div class="event-meta">
                                        <i class="fas fa-calendar"></i>
                                        <span>Friday, May 30, 5:00 - 6:00 PM</span>
                                    </div>
                                    <div class="event-meta">
                                        <i class="fas fa-video"></i>
                                        <span>Virtual</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="event-card">
                        <div class="event-content">
                            <img src="Assets/Images/get together.jpg" alt="event image" class="event-image">
                            <div class="event-details">
                                <h3 class="event-title">Quarlty get together</h3>
                                <div class="event-info">
                                    <div class="event-meta">
                                        <i class="fas fa-calendar"></i>
                                        <span>Friday, May 30, 1:00 - 2:00 PM</span>
                                    </div>
                                    <div class="event-meta">
                                        <i class="fas fa-map-marker-alt"></i>
                                        <span>In Person</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </section> --}}

            <!-- Meetings -->
            <section class="section">
                <h2 class="section-title">Meetings</h2>
                <div class="meeting-card">
                    <div class="meeting-header">
                        <div class="meeting-info">
                            <div class="meeting-icon">
                                <i class="fas fa-users"></i>
                            </div>
                            <h3 class="meeting-title">Policy making meeting</h3>
                        </div>
                        <button class="join-btn">Join meet</button>
                    </div>

                    <div class="meeting-details">
                        <div class="meeting-meta">
                            <i class="fas fa-calendar"></i>
                            <span>Friday, Jun 6, 4:00-6:00 IST</span>
                        </div>
                        <div class="meeting-meta">
                            <i class="fas fa-map-marker-alt"></i>
                            <span>In person</span>
                        </div>
                    </div>

                    <div class="meeting-footer">
                        <span class="status-badge green">Starts in 10 minutes</span>
                        <span class="status-badge outline">
                            <i class="fas fa-video"></i>
                            Virtual Event
                        </span>
                    </div>
                </div>
            </section>

         
            <div id="verifyMobileModal"
                class="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center p-4 z-50"
                style="display: none;">
                <div class="bg-white rounded-2xl shadow-2xl p-8 w-full max-w-md">
                    <div class="text-center mb-8">
                        <div
                            class="w-20 h-20 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full flex items-center justify-center mx-auto mb-4 shadow-lg">
                            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M5.121 17.804A7 7 0 0112 15a7 7 0 016.879 2.804M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                            </svg>
                        </div>
                        <h2 class="text-3xl font-bold text-gray-800 mb-2">Complete Your Profile</h2>
                        <p class="text-gray-600 text-sm">Verify your mobile number to proceed</p>
                    </div>

                    <form id="verifyMobileForm" class="space-y-8">
                        {{#unless user.mobile_verified}}
                        <div id="mobileSection" class="space-y-4">
                            <input type="tel" id="mobile_no" name="mobile_no"
                                class="w-full border-2 border-gray-200 rounded-xl p-4 text-lg font-semibold focus:border-blue-500 focus:ring-4 focus:ring-blue-200 outline-none transition-all"
                                placeholder="Enter 10-digit mobile number" required>
                            <button type="button" id="sendOtpBtn"
                                class="w-full bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-semibold py-4 px-6 rounded-2xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl focus:outline-none focus:ring-4 focus:ring-blue-200"
                                disabled>
                                <span class="flex items-center justify-center space-x-2">
                                    <span>Send OTP</span>
                                </span>
                            </button>
                            <p class="error text-red-600 text-sm text-center" id="mobileError" style="display: none;">
                            </p>
                            <div id="otpSection" class="flex justify-center space-x-3 hidden">
                                <input type="number" class="otp-input" maxlength="1" min="0" max="9" placeholder=""
                                    autocomplete="off">
                                <input type="number" class="otp-input" maxlength="1" min="0" max="9" placeholder=""
                                    autocomplete="off">
                                <input type="number" class="otp-input" maxlength="1" min="0" max="9" placeholder=""
                                    autocomplete="off">
                                <input type="number" class="otp-input" maxlength="1" min="0" max="9" placeholder=""
                                    autocomplete="off">
                                <input type="number" class="otp-input" maxlength="1" min="0" max="9" placeholder=""
                                    autocomplete="off">
                                <input type="number" class="otp-input" maxlength="1" min="0" max="9" placeholder=""
                                    autocomplete="off">
                            </div>
                            <button type="button" id="verifyOtpBtn"
                                class="w-full bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-semibold py-4 px-6 rounded-2xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl focus:outline-none focus:ring-4 focus:ring-blue-200 hidden"
                                disabled>
                                <span class="flex items-center justify-center space-x-2">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M5 13l4 4L19 7"></path>
                                    </svg>
                                    <span>Verify Code</span>
                                </span>
                            </button>
                            <div class="text-center hidden" id="resendSection">
                                <p class="text-gray-600 text-sm mb-2">Didn't receive the code?</p>
                                <button type="button" id="resendOtpBtn"
                                    class="text-blue-600 font-medium hover:text-blue-800 transition-colors duration-200 underline underline-offset-2">
                                    Resend code
                                </button>
                            </div>
                        </div>
                        {{/unless}}
                        {{#if user.mobile_verified}}
                        {{#unless user.has_password}}
                        <div id="passwordSection" class="space-y-4">
                            <label for="newPassword" class="block text-sm font-medium text-gray-700">New
                                Password</label>
                            <input type="password" id="newPassword" name="password"
                                class="w-full border-2 border-gray-200 rounded-xl p-4 text-lg font-semibold focus:border-blue-500 focus:ring-4 focus:ring-blue-200 outline-none transition-all"
                                placeholder="Enter new password" required>
                            <label for="confirmPassword" class="block text-sm font-medium text-gray-700">Confirm
                                Password</label>
                            <input type="password" id="confirmPassword" name="confirmPassword"
                                class="w-full border-2 border-gray-200 rounded-xl p-4 text-lg font-semibold focus:border-blue-500 focus:ring-4 focus:ring-blue-200 outline-none transition-all"
                                placeholder="Confirm password" required>
                            <button type="submit" id="setPasswordBtn"
                                class="w-full bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-semibold py-4 px-6 rounded-2xl transition-all duration-300 transform hover:scale-105 hover:shadow-xl focus:outline-none focus:ring-4 focus:ring-blue-200">
                                <span class="flex items-center justify-center space-x-2">
                                    <span>Set Password</span>
                                </span>
                            </button>
                            <p class="error text-red-600 text-sm text-center" id="passwordError" style="display: none;">
                            </p>
                        </div>
                        {{/unless}}
                        {{/if}}
                    </form>
                </div>

                <!-- OTP Success/Error Modal -->
                <div id="otpModal"
                    class="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center p-4 z-50 opacity-0 invisible transition-all duration-300">
                    <div class="bg-white rounded-3xl shadow-2xl p-8 w-full max-w-md transform scale-75 transition-all duration-300"
                        id="modalContent">
                        <div class="text-center">
                            <div id="success-code" class="hidden">
                                <div
                                    class="w-20 h-20 bg-gradient-to-br from-green-500 to-emerald-600 rounded-full flex items-center justify-center mx-auto mb-6 shadow-lg">
                                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor"
                                        viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M5 13l4 4L19 7"></path>
                                    </svg>
                                </div>
                                <h2 class="text-2xl font-bold text-green-600 mb-4">Code Verified!</h2>
                                <p class="text-gray-600 mb-6">Your OTP code has been verified successfully.</p>
                            </div>
                            <div id="error-code" class="hidden">
                                <div
                                    class="w-20 h-20 bg-gradient-to-br from-red-500 to-red-600 rounded-full flex items-center justify-center mx-auto mb-6 shadow-lg">
                                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor"
                                        viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M12 9v2m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                    </svg>
                                </div>
                                <h2 class="text-2xl font-bold text-red-600 mb-4">Verification Failed!</h2>
                                <p class="text-gray-600 mb-6">The code you entered is incorrect. Please try again.</p>
                            </div>
                            <div
                                class="bg-gradient-to-r from-blue-50 to-purple-50 border-2 border-dashed border-blue-300 rounded-2xl p-6 mb-6">
                                <div class="text-3xl font-bold text-blue-600 letter-spacing-widest" id="modalOtpCode">
                                    ------</div>
                                <p class="text-sm text-gray-500 mt-2">Verification Code</p>
                            </div>
                            <div class="flex gap-3">
                                <button id="closeModalBtn"
                                    class="flex-1 bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold py-3 px-4 rounded-xl transition-all duration-300 transform hover:scale-105 focus:outline-none focus:ring-4 focus:ring-gray-200">
                                    Close
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <!-- Add New Member Modal -->
    <div id="addMemberModal" class="modal-overlay" style="display:none;">
        <div class="modal-content">
            <div class="modal-header">
                <span>Add New Member</span>
                <button class="modal-close-btn" id="closeAddMemberModal">&times;</button>
            </div>
            <form class="add-member-form" id="addMemberForm">
                <label>Name</label>
                <input type="text" class="add-member-input" id="memberName" placeholder="Enter name" required>
                <label>Number</label>
                <input type="tel" class="add-member-input" id="memberNumber" placeholder="Enter number" required>
                <label>Email</label>
                <input type="email" class="add-member-input" id="memberEmail" placeholder="Enter email" required>
                <button type="submit" class="send-invite-btn">Send Invite</button>
            </form>
        </div>
    </div>
    <div id="Kycmodel" class="modal-overlay" style="display:none;">
        <div class="modal-content">
            <div class="modal-header">
                <span>First You have to do KYC than you get the access!!</span>
              
            </div>
            <button class="kyc-required-btn" >Get KYC</button>
        </div>
    </div>


    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    
    <script src="/js/all.js"></script>
    <script>

     
        // Show notification
        function showNotification(message, type = 'info') {
            toastr.options = { timeOut: 3000, positionClass: 'toast-top-right' };
            if (type === 'success') toastr.success(message);
            else if (type === 'error') toastr.error(message);
            else toastr.info(message);
        }

        // Debounce function to limit API calls
        function debounce(func, wait) {
            let timeout;
            return function (...args) {
                clearTimeout(timeout);
                timeout = setTimeout(() => func.apply(this, args), wait);
            };
        }

        // Function to render groups
        function renderGroups(containerId, groups, noResultsId, isDiscover = false) {
            const container = document.getElementById(containerId);
            const noResults = document.getElementById(noResultsId);
            
            if (!container || !noResults) {
                console.error(`Cannot find container (${containerId}) or noResults (${noResultsId}) element`);
                showNotification('Error rendering groups. Please try again.', 'error');
                return;
            }

            
            const groupCards = container.querySelectorAll('.group-card');
            groupCards.forEach(card => card.remove());

            if (groups.length === 0) {
                noResults.style.display = 'block';
                return;
            }
            noResults.style.display  = 'none';
            groups.forEach(group => {
                const groupCard = document.createElement('div');
                groupCard.className = 'group-card';
                groupCard.setAttribute('data-group-name', group.g_name);
                groupCard.innerHTML = `
                    <img src="${group.g_cover}" alt="${group.g_name} Image" class="group-image">
                    <div class="group-details">
                        <h3 class="group-title">${group.g_name}</h3>
                        <p class="group-description">
                            ${isDiscover ? `${group.description} Join this ${group.g_type} community to connect with others.` : `Manage your ${group.g_type} community with ${group.total_mem} members.`}
                        </p>
                        ${isDiscover ? `
                            ${group.isJoined ? '<button class="joined-btn" disabled>Joined</button>' : 
                              group.isPending ? '<button class="pending-btn" disabled>Pending</button>' : 
                              `<button class="join-group-btn" data-group-id="${group._id}">Join Community</button>`}
                        ` : `
                            ${group.membershipType === 'admin' ? `<button class="manage-group-btn" data-group-id="${group._id}">Manage Community</button>` :  `<button class="leave-btn" data-group-id="${group._id}">Leave Community</button>`
                            }
                        `
                        <button class="manage-group-btn" data-group-id="${group._id}">Manage Community</button>
                        }

                    </div>
                `;
                container.appendChild(groupCard);
            });
        }

        // Function to fetch groups from backend
        async function fetchGroups(endpoint, query, containerId, noResultsId, isDiscover) {
            const loadingSpinner = document.getElementById(isDiscover ? 'discoverLoading' : 'myGroupsLoading');
            loadingSpinner.style.display = 'block';
            try {
                const response = await fetch(`${endpoint}?query=${encodeURIComponent(query)}`, {
                    method: 'GET',
                    headers: { 'Content-Type': 'application/json' },
                    credentials: 'include'
                });
                const result = await response.json();
                if (response.ok) {
                    renderGroups(containerId, result.groups, noResultsId, isDiscover);
                } else {
                    showNotification(result.message || 'Failed to fetch groups', 'error');
                }
            } catch (error) {
                console.error(`Error fetching ${isDiscover ? 'discover' : 'my'} groups:`, error);
                showNotification(`Error fetching groups: ${error.message}`, 'error');
            } finally {
                loadingSpinner.style.display = 'none';
            }
        }

        // Initialize on page load
        document.addEventListener('DOMContentLoaded', () => {
            // Initial rendering from server-side data
            // Discover groups search
            document.getElementById('discoverSearch').addEventListener('input', debounce((e) => {
                const query = e.target.value.trim();
                fetchGroups('/Groups/search/discover', query, 'discoverGroupsList', 'discoverNoResults', true);
            }, 300));

            // My groups search
            document.getElementById('myGroupsSearch').addEventListener('input', debounce((e) => {
                const query = e.target.value.trim();
                fetchGroups('/Groups/search/my', query, 'myGroupsList', 'myGroupsNoResults', false);
            }, 300));
        });
    </script>

    {{> profile}}
</body>

</html>