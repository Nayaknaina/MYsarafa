<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Records - Sarafa Community</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="/socket.io/socket.io.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
    <link rel="icon" href="/Assets/Images/logo.png" type="image/x-icon">
    <style>
        .payment-table {
            width: 100%;
            border-collapse: collapse;
            background: #fff;
            border-radius: 12px;
            overflow: hidden;
            font-size: 15px;
            box-shadow: 0 2px 8px rgba(47,149,255,0.07);
        }
        .filters-section {
            background: white;
            border-radius: 16px;
            padding: 7px;
            margin-bottom: 4px;
            box-shadow: 0 4px 20px rgba(47, 149, 255, 0.1);
            border: 1px solid #e5e7eb;
        }
        .filters-title {
            font-size: 20px;
            font-weight: 600;
            color: #111827;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .filters-title i {
            color: #2f95ff;
        }
        .filters-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            align-items: end;
        }
        .filter-group {
            display: flex;
            flex-direction: column;
        }
        .filter-label {
            font-size: 14px;
            font-weight: 500;
            color: #374151;
            margin-bottom: 8px;
        }
        .filter-input {
            padding: 12px 16px;
            border: 1px solid #d1d5db;
            border-radius: 8px;
            font-size: 14px;
            background: white;
            transition: border-color 0.2s;
        }
        .search-btn {
            padding: 12px 16px;
            border: 1px solid #d1d5db;
            border-radius: 8px;
            font-size: 14px;
            background: #2f95ff;
            color: #fff;
            cursor: pointer;
            transition: background 0.2s;
        }
        .filter-input:focus {
            outline: none;
            border-color: #2f95ff;
            box-shadow: 0 0 0 3px rgba(47, 149, 255, 0.1);
        }
        .payment-table th, .payment-table td {
            padding: 12px 8px;
            text-align: left;
            border-bottom: 1px solid #f1f5f9;
        }
        .payment-table th {
            background: #f8fafc;
            font-weight: 600;
            color: #374151;
        }
        .payment-table tr:last-child td {
            border-bottom: none;
        }
        .status-badge {
            background: #d1f7e6;
            color: #1b8c4a;
            padding: 4px 12px;
            border-radius: 14px;
            font-size: 12px;
            font-weight: 600;
        }
        .status-badge.pending {
            background: #fef3c7;
            color: #b45309;
        }
        .screenshot-btn {
            background: none;
            border: none;
            color: #2f95ff;
            font-size: 18px;
            cursor: pointer;
            transition: color 0.2s;
        }
        .screenshot-btn:hover {
            color: #551DFF;
        }
        .action-btn {
            background: #fff;
            border: 1px solid #d1d5db;
            color: #374151;
            border-radius: 8px;
            font-size: 14px;
            padding: 6px 10px;
            margin-right: 2px;
            cursor: pointer;
            transition: background 0.2s, border-color 0.2s;
        }
        .action-btn.primary {
            background: #2f95ff;
            color: #fff;
            border-color: #2f95ff;
        }
        .action-btn:hover {
            background: #f3f4f6;
            border-color: #9ca3af;
        }
        .action-btn.primary:hover {
            background: #1d7dd8;
        }
        @media (max-width: 700px) {
            .payment-table th, .payment-table td {
                padding: 8px 4px;
                font-size: 13px;
            }
            .payment-table {
                font-size: 13px;
            }
        }
        @media (max-width: 500px) {
            .payment-table, .payment-table thead, .payment-table tbody, .payment-table th, .payment-table td, .payment-table tr {
                display: block;
            }
            .payment-table thead {
                display: none;
            }
            .payment-table tr {
                margin-bottom: 16px;
                background: #fff;
                border-radius: 10px;
                box-shadow: 0 1px 3px rgba(47,149,255,0.07);
                padding: 8px;
            }
            .payment-table td {
                padding: 8px 6px;
                border-bottom: none;
                position: relative;
            }
            .payment-table td:before {
                content: attr(data-label);
                font-weight: 600;
                color: #374151;
                display: block;
                margin-bottom: 2px;
                font-size: 12px;
            }
        }
        .action-dropdown {
            position: relative;
            display: inline-block;
        }
        .action-main-btn {
            background: #fff;
            border: 1px solid #d1d5db;
            color: #374151;
            border-radius: 8px;
            font-size: 18px;
            padding: 6px 12px;
            cursor: pointer;
            transition: background 0.2s, border-color 0.2s;
        }
        .action-main-btn:hover {
            background: #f3f4f6;
            border-color: #9ca3af;
        }
        .action-dropdown-menu {
            display: none;
            position: absolute;
            right: 0;
            top: 110%;
            min-width: 120px;
            background: #fff;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(47,149,255,0.10);
            z-index: 10;
            flex-direction: column;
            padding: 6px 0;
        }
        .action-dropdown:hover .action-dropdown-menu,
        .action-main-btn:focus + .action-dropdown-menu {
            display: flex;
        }
        .action-dropdown-menu .action-btn {
            width: 100%;
            border: none;
            background: none;
            color: #374151;
            border-radius: 0;
            font-size: 15px;
            padding: 8px 16px;
            text-align: left;
            cursor: pointer;
            transition: background 0.2s;
            margin: 0;
        }
        .action-dropdown-menu .action-btn.primary {
            color: #2f95ff;
        }
        .action-dropdown-menu .action-btn:hover {
            background: #f3f4f6;
        }
        .action-dropdown-menu .action-btn.primary:hover {
            background: #e7f3ff;
        }
        @media (max-width: 500px) {
            .action-dropdown-menu {
                min-width: 100px;
                right: 0;
                left: auto;
            }
        }
    </style>
</head>
<body data-user-id="{{user._id}}">
    {{> header}}
   
   <div class="main-container">
        {{> sidebar}}
    <main class="main-content">
        <div class="filters-section">
            <h3 class="filters-title">
                <i class="fas fa-filter"></i>
                Filter Payments
            </h3>
            <div class="filters-row">
                <div class="filter-group">
                    <label class="filter-label">Search Member</label>
                    <input type="text" class="filter-input" id="searchMember" placeholder="Enter name or ID">
                </div>
                <div class="filter-group">
                    <label class="filter-label">Payment Method</label>
                    <select class="filter-input" id="filterMethod">
                        <option value="">All Methods</option>
                        <option value="UPI">UPI</option>
                        <option value="Bank Transfer">Bank Transfer</option>
                        <option value="Cash">Cash</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label class="filter-label">Date Range</label>
                    <input type="date" class="filter-input" id="filterDate">
                </div>
                <div class="filter-group">
                    <button class="search-btn" id="applyFilters">Apply Filters</button>
                </div>
            </div>
        </div>
        <div class="payment-records-list" style="margin-bottom:32px;">
            <div style="overflow-x:auto;">
                <table class="payment-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Group</th>
                            <th>UPI ID</th>
                            <th>Date</th>
                            <th>Time</th>
                            <th>Method</th>
                            <th>Amount</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody id="paymentTableBody">
                        {{#each payments}}
                        <tr>
                            <td data-label="Name">{{this.user.f_name}} {{this.user.l_name}}</td>
                            <td data-label="Group">{{this.group.g_name}}</td>
                            <td data-label="UPI ID">{{this.upiId}}</td>
                            <td data-label="Date">{{this.date}}</td>
                            <td data-label="Time">{{this.time}}</td>
                            <td data-label="Method">{{this.method}}</td>
                            <td data-label="Amount">₹{{this.amount}}</td>
                            <td data-label="Status">
                            <span class="status-badge {{#if this.isVerified}}green{{else}}pending{{/if}}">
                                {{#if this.isVerified}}Verified{{else}}Pending{{/if}}
                            </span>
                            </td>
                         <td data-label="Actions">
    <div class="action-dropdown">
        <button class="action-main-btn" title="Actions">
            <i class="fas fa-ellipsis-h"></i>
        </button>
        <div class="action-dropdown-menu">
            <a href="{{this.screenshotUrl}}" target="_blank" class="action-btn" title="View">
                <i class="fas fa-eye"></i> View
            </a>
            <a href="{{this.screenshotUrl}}" download class="action-btn" title="Download">
                <i class="fas fa-download"></i> Download
            </a>

            {{#if this.isVerified}}
                <button class="action-btn primary verify-btn" data-payment-id="{{this._id}}" title="Verify" disabled>
                    <i class="fas fa-check"></i> Verify
                </button>
                <button class="action-btn reupload-btn" data-payment-id="{{this._id}}" title="Reupload">
                    <i class="fas fa-undo"></i> Reupload
                </button>
            {{else}}
                <button class="action-btn primary verify-btn" data-payment-id="{{this._id}}" title="Verify">
                    <i class="fas fa-check"></i> Verify
                </button>
                <button class="action-btn reupload-btn" data-payment-id="{{this._id}}" title="Reupload" disabled>
                    <i class="fas fa-undo"></i> Reupload
                </button>
            {{/if}}
        </div>
    </div>
</td>

                        </tr>
                        {{else}}
                        <tr>
                            <td colspan="9">No payment records found.</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </main>
</div>
    <script>
        $(document).ready(function () {
            function showNotification(message, type) {
                toastr[type](message);
            }

            // Handle Verify button
            $(document).on('click', '.verify-btn', async function () {
                const paymentId = $(this).data('payment-id');
                try {
                    const response = await fetch(`/pay/verify/${paymentId}`, {
                        method: 'POST',
                        credentials: 'include',
                        headers: {
                            'Content-Type': 'application/json',
                            'Authorization': `Bearer ${document.cookie.split('token=')[1]?.split(';')[0]}`
                        }
                    });
                    const result = await response.json();
                    if (response.ok && result.success) {
                        showNotification('Payment verified successfully', 'success');
                        setTimeout(() => location.reload(), 1000);
                    } else {
                        showNotification(result.message || 'Failed to verify payment', 'error');
                    }
                } catch (error) {
                    showNotification('Error verifying payment: ' + error.message, 'error');
                }
            });

            // Handle Reupload button
            $(document).on('click', '.reupload-btn', async function () {
                const paymentId = $(this).data('payment-id');
                try {
                    const response = await fetch(`/pay/unverify/${paymentId}`, {
                        method: 'POST',
                        credentials: 'include',
                        headers: {
                            'Content-Type': 'application/json',
                            'Authorization': `Bearer ${document.cookie.split('token=')[1]?.split(';')[0]}`
                        }
                    });
                    const result = await response.json();
                    if (response.ok && result.success) {
                        showNotification('Payment marked for reupload', 'success');
                        setTimeout(() => location.reload(), 1000);
                    } else {
                        showNotification(result.message || 'Failed to mark payment for reupload', 'error');
                    }
                } catch (error) {
                    showNotification('Error marking payment for reupload: ' + error.message, 'error');
                }
            });

            // Handle filters (client-side for simplicity)
            $('#applyFilters').click(function () {
                const searchMember = $('#searchMember').val().toLowerCase();
                const filterMethod = $('#filterMethod').val();
                const filterDate = $('#filterDate').val();

                $('#paymentTableBody tr').each(function () {
                    const name = $(this).find('td[data-label="Name"]').text().toLowerCase();
                    const method = $(this).find('td[data-label="Method"]').text();
                    const date = $(this).find('td[data-label="Date"]').text();

                    const matchesSearch = searchMember ? name.includes(searchMember) : true;
                    const matchesMethod = filterMethod ? method === filterMethod : true;
                    const matchesDate = filterDate ? date === filterDate : true;

                    $(this).toggle(matchesSearch && matchesMethod && matchesDate);
                });
            });
        });
    </script>
     <script src="/js/all.js"></script>
</body>
</html>